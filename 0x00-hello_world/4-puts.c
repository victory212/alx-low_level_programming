#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	puts("\n");

return (0);
}
