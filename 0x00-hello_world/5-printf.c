#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0
 *
 */

int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);

return (0);
}
