#include "main.h"

/**
 * _abs - Program to conpute the abs value
 * @r: type returned
 * Return: the absolute value
 */

int _abs(int r)
{

	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
